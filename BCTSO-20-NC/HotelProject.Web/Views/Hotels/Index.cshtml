@model List<Hotel>
<div class="row">
    <a class="btn btn-primary w-25 mt-3 mb-3" asp-controller="Hotels" asp-action="Create">ახალი სასტუმრო</a>
</div>
<div class="row">
    <table class="table table-bordered table-striped">
        <thead>
            <tr>
                <th>სახელი</th>
                <th>რეიტინგი</th>
                <th>ქვეყანა</th>
                <th>ქალაქი</th>
                <th>მისამართი</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var hotel in Model)
            {
                <tr>
                    <td>@hotel.Name</td>
                    <td>@hotel.Rating</td>
                    <td>@hotel.Country</td>
                    <td>@hotel.City</td>
                    <td>@hotel.PhyisicalAddress</td>
                    <td>
                        <a asp-controller="Hotels" asp-action="Update" asp-route-id="@hotel.Id" class="btn btn-warning">რედაქტირება</a>
                        <a asp-controller="Hotels" asp-action="Delete" asp-route-id="@hotel.Id" class="btn btn-danger">წაშლა</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>